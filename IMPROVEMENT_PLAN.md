# راهنمای جامع بهبود نتایج — پیش‌بینی عود بیوشیمیایی سرطان پروستات (BCR)

> این سند بر اساس تحلیل کامل پایپ‌لاین فعلی (نوت‌بوک‌های ۰۱ تا ۰۹ و ماژول‌های `src/`) نوشته شده است.

---

## ۱) وضعیت فعلی و ریشه‌یابی مشکلات

### نتایج فعلی

| ارزیابی | معیار اصلی | وضعیت |
|---|---|---|
| CV داخلی (۵-فولد) | AUC = 0.834 | ظاهراً خوب |
| تست نگه‌داشته (n=86، ۱۲ مثبت) | **AUC = 0.672** (CI: 0.52–0.82) | افت شدید |
| اعتبارسنجی خارجی GSE70769 (n=93) | **AUC = 0.42–0.53** | شکست کامل (بدتر از تصادفی) |
| اختصاصیت در آستانه 0.0398 | 48.6% (۳۸ مثبت کاذب از ۷۴) | از نظر بالینی غیرقابل قبول |

### پنج ریشه اصلی مشکل

1. **نشت اطلاعات در انتخاب ویژگی (مهم‌ترین مشکل داخلی):**
   انتخاب ویژگی با PSO روی *کل* داده آموزش (نوت‌بوک ۰۴) انجام شده و بعد مدل نهایی روی همان ویژگی‌ها آموزش دیده است. عدد CV برابر 0.834 حاصل همین بیش‌برازش است؛ در nested-CV صادقانه، PSO حتی از MI-only هم ضعیف‌تر بود (0.680 در برابر 0.747).

2. **انتخاب ۳۰ ژن از ۱۹هزار ویژگی با فقط ۴۶ نمونه مثبت:**
   انتخاب ۳۰ ژن از میان ~۱۹,۰۰۰ ویژگی با ۳۴۳ نمونه آموزش، عملاً انتخاب نویز است. این ژن‌ها (MED8، HPR، BZW2 و…) هیچ پایداری بین‌کوهورتی ندارند — به همین دلیل در GSE70769 هیچ سیگنالی منتقل نمی‌شود.

3. **عدم تطابق پلتفرم و شیوع کلاس بین دو کوهورت:**
   TCGA با RNA-Seq و شیوع عود ۱۳.۵٪ است؛ GSE70769 با میکروآری و شیوع ~۴۸٪. بدون نرمال‌سازی متقاطع پلتفرم و بدون محدود کردن فضای ویژگی به ژن‌های مشترکِ *قبل از* انتخاب، اعتبارسنجی خارجی محکوم به شکست است.

4. **عدم توازن شدید کلاس با راه‌حل حداقلی:**
   فقط `scale_pos_weight` استفاده شده. آستانه 0.0398 برای رسیدن به حساسیت ۷۵٪ یعنی کالیبراسیون احتمال به‌کلی خراب است.

5. **انتخاب مدل و تنظیم هایپرپارامتر نامتناسب:**
   XGBoost با `max_depth=1, gamma=5, reg_lambda=20` عملاً مجموعه‌ای از stump است؛ یعنی مدل سیگنال واقعی پیدا نکرده. Random Forest با CV بهتر بود (0.858) ولی انتخاب نشد. ماژول Optuna (۶۷۸ خط) اصلاً استفاده نمی‌شود.

---

## ۲) راه‌حل‌ها به ترتیب اولویت

### اولویت ۱ — اصلاح روش‌شناسی (بیشترین اثر، بدون داده جدید)

**۱.۱. انتخاب ویژگی واقعاً تودرتو (Nested):**
هر مرحله انتخاب (Variance → MI → انتخاب نهایی) باید *داخل* هر فولد بیرونی CV انجام شود. مدل نهایی فقط ویژگی‌هایی را استفاده کند که در ≥۶۰٪ فولدها/بوت‌استرپ‌ها انتخاب شده‌اند.

```python
# به جای: select once on full train → tune → evaluate
for train_idx, test_idx in outer_cv.split(X, y):
    X_tr = X.iloc[train_idx]
    selected = full_selection_pipeline(X_tr, y[train_idx])   # داخل فولد
    model.fit(X_tr[selected], y[train_idx])                  # فقط همان ویژگی‌ها
```

**۱.۲. جایگزینی PSO با Stability Selection (انتخاب پایدار):**
در nested-CV، PSO هم بهتر نبود هم واریانسش بیشتر بود. جایگزین استاندارد:

```python
def stability_selection(X, y, n_boot=100, alpha=0.3, threshold=0.6):
    """L1-logistic روی ۱۰۰ بوت‌استرپ؛ ژن‌هایی که در ≥60% تکرارها انتخاب می‌شوند."""
    freq = pd.Series(0.0, index=X.columns)
    for b in range(n_boot):
        idx = resample(range(len(X)), stratify=y, random_state=b)
        lsvc = LogisticRegression(penalty="l1", solver="liblinear", C=1/alpha)
        lsvc.fit(X.iloc[idx], y.iloc[idx])
        freq += (np.abs(lsvc.coef_[0]) > 1e-6)
    return freq / n_boot, freq[freq >= threshold].index.tolist()
```

**۱.۳. Elastic Net به‌عنوان مدل اصلی (استاندارد ژنومیک p≫n):**
رگرسیون لجستیک با جریمه ترکیبی، برای داده‌ای با ۱۹هزار ویژگی و ۳۴۳ نمونه، به‌طور قابل‌اتکایی از XGBoost عمیق بهتر تعمیم می‌یابد. مدل‌های درختی را به‌عنوان مقایسه نگه دارید.

```python
enet = LogisticRegressionCV(
    Cs=20, l1_ratios=[0.1, 0.5, 0.9], penalty="elasticnet",
    solver="saga", cv=StratifiedKFold(5, shuffle=True),
    scoring="roc_auc", class_weight="balanced", max_iter=5000,
)
```

**۱.۴. کاهش تعداد ویژگی نهایی: ۳۷ → ۱۰–۱۵:**
قانون سرانگشتی: حداکثر `n_positive / 5` ویژگی (۴۶/۵ ≈ ۹–۱۰). مدل کوچک‌تر = فاصله اطمینان تنگ‌تر = تعمیم بهتر.

**۱.۵. Repeated Stratified CV برای تخمین صادقانه:**
به جای یک بار ۵-فولد، `RepeatedStratifiedKFold(5, 10)` — با ۱۲ مثبت در تست، یک تقسیم واحد اساساً نویز است. گزارش: mean ± std روی ۵۰ تکرار.

**۱.۶. مدل پایه بالینی (الزامی برای انتشار):**
مدل فقط-بالینی (Gleason + PSA + T-stage + Margin + LN) را بسازید. اگر مدل ژنومیک از این پایه بهتر نبود، ارزش افزوده ندارد. در TCGA-PRAD معمولاً مدل بالینی AUC ≈ 0.70–0.75 می‌گیرد — این عدد هدف واقع‌بینانه است.

### اولویت ۲ — نجات اعتبارسنجی خارجی

**۲.۱. آموزش در فضای ویژگی مشترک از ابتدا:**
قبل از هر انتخاب ویژگی، فضای ژن‌های مشترک TCGA ∩ GSE70769 را محاسبه و انتخاب را فقط در همین فضا انجام دهید. (الان ۱۳ ویژگی از ۳۷ در کوهورت خارجی غایب‌اند — یعنی مدل خارجی اصلاً همان مدل نیست.)

**۲.۲. نرمال‌سازی متقاطع پلتفرم در سطح بیمار:**
روش صحیح برای حذف اثر پلتفرم، z-score کردن *هر بیمار* روی مجموعه ژن‌های مشترک است (نه فقط کل ستون‌ها):

```python
def patient_zscore(df_common_genes):
    # هر سطر (بیمار) روی ژن‌های مشترک استاندارد می‌شود → مقیاس پلتفرم حذف می‌شود
    return df_common_genes.sub(df_common_genes.mean(axis=1), axis=0) \
                          .div(df_common_genes.std(axis=1), axis=0)
```
سپس ComBat با پارامترهای frozen (آموزش روی TCGA، اعمال روی GSE70769).

**۲.۳. اعتبارسنجی مدل بالینی در کوهورت خارجی:**
ویژگی‌های بالینی (Gleason، PSA، مرحله) بین پلتفرم‌ها بدون مشکل منتقل می‌شوند. اگر مدل بالینی در GSE70769 هم AUC ≈ 0.65+ بگیرد، پایپ‌لاین اعتبارسنجی سالم است و مشکل از ژن‌هاست، نه زیرساخت.

**۲.۴. تفسیر صحیح AUC با شیوع معکوس:**
AUC مستقل از آستانه است، پس شیوع ۴۸٪ vs ۱۳.۵٪ AUC را خراب نمی‌کند — اما برای گزارش حساسیت/اختصاصیت، آستانه باید با `logit(p) - logit(prevalence_train) + logit(prevalence_external)` بازکالیبره شود.

### اولویت ۳ — بهبود مدل‌سازی

**۳.۱. فعال‌سازی ماژول Optuna:**
`src/optimization.py` (TPESampler + MedianPruner) از قبل نوشته شده ولی مرده است. جایگزین `RandomizedSearchCV(n_iter=20)` کنید: ۱۲۰–۲۰۰ trial با pruning.

**۳.۲. Ensemble رأی‌گیری نرم:**
`VotingEnsemble(XGB + LightGBM + CatBoost)` موجود است؛ میانگین‌گیری سه مدل هم‌خانواده واریانس را ~۳۰٪ کم می‌کند.

**۳.۳. کالیبراسیون احتمال:**
`CalibratedClassifierCV(method="isotonic", cv=5)` روی مدل نهایی. با این کار آستانه 0.5 دوباره معنادار می‌شود و آن آستانه 0.0398 غیربالینی حذف می‌شود.

**۳.۴. بهینه‌سازی بر اساس PR-AUC (نه ROC-AUC):**
با شیوع ۱۳.۵٪، `average_precision` معیار صادق‌تر داخل CV است؛ ROC-AUC در نامتوازن بودن خوش‌بینانه است.

### اولویت ۴ — افزایش داده (اگر امکان داشت)

کوهورت‌های اضافی قابل استفاده از GEO:
- **GSE54460** (n≈106، RP + follow-up BCR)
- **GSE46691** (n~=525، مستقل، BCR)
- **GSE106453** (n≈245، تکرارهای مستقل از Kwa)
- **E-MTAB-6128** (ArrayExpress، n~=118)

استراتژی: آموزش روی TCGA + اعتبارسنجی روی *چند* کوهورت خارجی. یک امضای واقعی باید در ≥۲ کوهورت مستقل کار کند.

### اولویت ۵ — گزارش‌دهی صادقانه و کاربرد بالینی

1. **README را تصحیح کنید** — اعداد فعلی (AUC=0.818 و بالانس‌دقت 76.4%) با خروجی‌های واقعی (0.672 و 61.8%) نمی‌خوانند؛ برای داوری مجدد کشنده است.
2. **Decision Curve Analysis (DCA):** فایده خالص در بازه آستانه بالینی ۱۰–۳۰٪ — این است که مقاله Q1 را می‌سازد، نه AUC.
3. **نمودار کالیبراسیون + Brier score** به ارزیابی نهایی اضافه شود.
4. **CI بوت‌استرپ برای همه معیارها** (الان فقط برای AUC هست).
5. تکرارپذیری: seed در همه سطوح + `pyreadr`/hash دیتاست.

---

## ۳) انتظار واقع‌بینانه پس از اصلاحات

| سناریو | AUC داخلی انتظاری | AUC خارجی انتظاری |
|---|---|---|
| وضع فعلی | 0.67 (تست) | 0.42–0.53 |
| پس از اصلاح روش‌شناسی (فقط داخل) | 0.68–0.72 با CI تنگ‌تر | — |
| + مدل بالینی + ژن پایدار مشترک | 0.72–0.75 | 0.60–0.68 |
| + کوهورت‌های اضافی | 0.72–0.78 | 0.63–0.70 |

> نکته: ادعای AUC > 0.85 با ۴۲۹ نمونه و ۱۹هزار ویژگی تقریباً همیشه نشت است. AUC خارجی 0.65 با مدل پایدار و چند-کوهورتی، برای Q1 قابل دفاع‌تر از 0.85 داخلی غیرقابل تکرار است.

---

## ۴) نقشه اجرا (فایل‌های آماده)

| فایل | محتوا |
|---|---|
| `core/src/improved_pipeline.py` | Stability selection، ElasticNet کالیبره، nested-CV صحیح، اعتبارسنجی متقاطع پلتفرم، مدل پایه بالینی |
| `core/notebooks/10_Improved_Model.ipynb` | اجرای گام‌به‌گام همه اصلاحات بالا |

**ترتیب اجرای پیشنهادی نوت‌بوک ۱۰:**
1. مدل پایه بالینی → AUC مرجع
2. ElasticNet با stability selection داخل nested-CV
3. مقایسه با XGBoost/Optuna (فعال‌سازی optimization.py)
4. اعتبارسنجی خارجی در فضای مشترک با patient-zscore + ComBat frozen
5. DCA + کالیبراسیون + بازنویسی README با اعداد واقعی
